import type { NextConfig } from 'next'

const nextConfig: NextConfig = {
  /* config options here */
  experimental: {
    // Puede que necesites esto en algunas versiones
    optimizeCss: true
  }
}

export default nextConfig
